# Dice game solution

This is an application created with Java and Gradle using the Intellij IDE.

In its current state, the game is unfinished.

The idea is to have a game that runs on the command line, where the two players can take turns and provide inputs.

## TODO
- Finish class creation
- Create game in main (new commit)
- Fix bugs from manual testing (new commit)
- Unit testing (new commit)